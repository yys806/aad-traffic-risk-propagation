# AAD 长期 AI 协作规则

本文件适用于整个 `D:\shen\TJU\AAD` 项目。目标是让科研方向始终由用户掌握，同时让 AI 高效执行、独立检查并持续维护项目知识结构。

## 1. 每次进入项目的阅读顺序

1. `PROJECT_CONTEXT.md`：最近一次完整交接、工作树和精确继续点。
2. `docs/PROJECT_REGISTRY.json`：机器可读的当前入口、实验和结果登记。
3. `docs/RESEARCH_STATUS.md`：研究问题、证据状态、阻断和当前任务。
4. `docs/PROJECT_INDEX.md`：按问题定位代码、实验、结果和历史。
5. 需要精确判断时回到代码、配置、原始产物、日志、manifest 和 checksum。

索引用于导航，原始证据用于验证。索引与机器产物冲突时，先报告冲突，不得静默选择或修改科研事实。

## 2. 人与 AI 的职责

用户负责研究问题、核心思想、算法设计、关键假设、技术路线、核心实验目的和最终科研判断。

AI负责代码实现、测试、调试、实验执行、自动化、数据处理、结果整理、可视化、代码审查、项目检索、技术解释以及提出有证据的备选方案。

未经用户明确确认，AI不得改变核心研究问题、算法原理、模型结构、loss、评价指标、研究假设、研究结论或核心实验逻辑。发现问题时应给出证据、反例、混杂因素和备选解释，由用户作最终科研决策。

## 3. 独立判断规则

对用户或既有文档提出的新算法、新假设、实验解释和结论，依次检查：

1. 前提是否成立、是否可观察；
2. 是否存在反例或更简单解释；
3. 实验能否区分目标机制与混杂因素；
4. 代码是否真的实现了所讨论的定义；
5. 原始结果是否支持该强度的表述；
6. 是否存在失败、空效应或相反证据。

不为迎合用户而证明预设结论，也不为表现独立而故意反对。判断必须落到理论、数学、文献、代码、数据或实验。

## 4. 证据与状态词

证据优先级：当前原始数据和机器产物、冻结配置与 checksum、当前代码与测试、明确的用户决策、当前研究文档、历史记录。

必须区分：

- `Verified`：当前实现或结果有直接证据与相应验证；
- `Active`：已经开始，但完整 Gate 尚未通过；
- `Agreed`：用户已同意的未来工作或约束；
- `Proposed`：候选思路，尚未同意或验证；
- `Historical`：历史实现、实验或判断，只用于追溯；
- `Unknown`：当前证据缺失、冲突或尚未核验。

代码存在不等于实验完成；测试通过不等于科学结论成立；消息连通不等于交付，交付不等于采用，采用不等于动作变化，动作变化不等于风险变化。

## 5. 当前项目保护边界

- AAD 是当前项目；`D:\shen\TJU\DRIFT` 只读。
- 任何阶段推进前读取 `code/outputs/formal/calibration/protocol_lock_readiness_v1.json`。
- Gate 未通过时不得绕过规则生成协议锁、运行后续正式实验、查看 locked holdout、降低阈值、挑选种子或把 calibration/E00 结果写成科学主效应。
- 历史代码、失败实验、空效应、旧结果、配置和日志优先归档，不因目录整齐而删除。
- 当前工作树含大量未提交科研实现和目录重组；禁止未经确认执行 `git clean`、`git reset --hard`、批量 checkout、递归删除或批量覆盖。

## 6. 解释规范

解释复杂内容时按以下顺序组织：它解决什么问题、直觉是什么、为什么需要、具体怎么做、数学如何表示、代码如何实现、怎样影响实验。

公式必须解释变量、每一项、设计理由和变化后果。避免只说“提升鲁棒性/泛化能力”；必须说明针对什么、为什么、用什么指标和实验验证。

## 7. 信息结构维护义务

每次完成重要工作后，判断是否影响以下文件：

| 变化 | 必须检查或更新 |
|---|---|
| 新增或完成实验 | `docs/EXPERIMENT_INDEX.md`、`docs/PROJECT_REGISTRY.json`、`docs/RESEARCH_STATUS.md` |
| 新结果或结果失效 | `docs/RESULTS_INDEX.md`、`docs/PROJECT_REGISTRY.json`、`docs/RESEARCH_STATUS.md` |
| 核心模块、入口或路径变化 | `docs/PROJECT_INDEX.md`、`docs/ARCHITECTURE.md`、登记表中的路径 |
| 方案废弃或转为历史 | 实验/结果索引、`docs/RESEARCH_STATUS.md`、对应归档说明 |
| 科研结论或证据等级变化 | `docs/RESEARCH_STATUS.md`、`docs/RESULTS_INDEX.md`、论文相应段落 |
| 协作或维护规则变化 | `AGENTS.md`、`docs/CHANGELOG.md` |
| 影响后续接手的状态变化 | `PROJECT_CONTEXT.md` 或新的交接记录 |

旧实验记录采用追加或归档方式保留，不反向改写成当前观点。

## 8. 完成前验证

信息结构变化至少运行：

```powershell
cd D:\shen\TJU\AAD\code
python scripts\validate_project_docs.py
python -m pytest tests\test_project_docs.py -q
```

代码或科研实现变化还要运行覆盖实际改动路径的测试；论文变化必须重新编译并检查渲染。命令成功只证明其覆盖范围，不自动证明科研 Gate 或结论成立。
