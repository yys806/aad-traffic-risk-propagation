# Historical Pilot modules

This package preserves the early DRIFT risk-event, propagation, feasibility,
communication and nonlocal Pilot implementation. It remains runnable for
reproduction and historical comparison, but it is not the current formal
experiment implementation.

Commands are under `code/scripts/legacy/`; tests are under
`code/tests/legacy/`. Current formal modules remain directly under
`riskprop/`.
