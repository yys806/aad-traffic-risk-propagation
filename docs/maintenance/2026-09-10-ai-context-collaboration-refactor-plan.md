# AI 上下文与长期协作层收口计划

## 目标

在不继续改变科研算法、指标、数据语义和实验变量的前提下，完成阶段化工程迁移，建立可由 ChatGPT 快速读取的 `AI_CONTEXT/`，并把长期 Codex 职责固化到根目录 `AGENTS.md`。

## 已确认范围

- 研究者于 2026-09-10 明确批准：pNEUMA 前车推断与人工复核退役，pNEUMA 当前仅承担城市运动学校准。
- 当前阶段名称使用“阶段 X.X”，机器标识使用 `stage_X_Y`；旧编号和旧结果保留在归档与历史证据中。
- 本次只执行工程迁移、知识结构整理、校验增强和 Git 收口，不运行新的科学实验，不生成协议锁，不进入阶段 1.1。

## 执行步骤

1. 核验 2026-09-09 暂存迁移的代码、数据、证据和归档边界，修正清单与文档漂移。
2. 以当前源码、配置、机器注册表、便携证据和本机输出为依据建立九个 `AI_CONTEXT` 文件。
3. 重写 `AGENTS.md`，只保留跨任务稳定规则，并明确后续不得由 Codex 自主修改。
4. 为 `AI_CONTEXT` 的文件集合、必需主题和 Git 跟踪状态增加自动一致性校验与测试。
5. 运行文档、证据、全量测试、编译、Git diff、敏感信息和纯 Git 克隆验证。
6. 按逻辑提交迁移与协作层，推送 `main`。

## 停止条件

- 任一校验失败且无法在本次授权范围内修复时，不提交或推送 broken state。
- 若发现新的科研定义冲突，只记录事实和证据，停止对应科研逻辑修改并等待研究者决定。
