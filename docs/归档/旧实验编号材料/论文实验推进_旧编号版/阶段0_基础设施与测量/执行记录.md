# 阶段 0 执行记录

本文件按日期追加阶段级执行事实。E00 的详细记录保存在 `E00/执行记录.md`。

## 2026-09-07

E00 真实 SUMO 最小四格包、五表封存、独立只读复算、路径/时间/车辆生命周期审计和第二 clean 环境结构复算均通过。E00 Gate 判定为 passed，但 `scientific_claim_eligible=false`；下一步仅进入现实数据校准与 E01 冻结，不启动 E02。

同日开始执行现实 calibration：数据清单升级为 v2 并纳入 OSM 快照；NGSIM 全量 calibration 测量完成；pNEUMA 候选地图匹配与 300 条离线人工 QA 包完成；SPMD 仅 calibration trips 的 `RV_RX` 重扫完成；E17 现实参考与 E01 预锁真值包完成。

机器可读准入文件 `code/outputs/formal/calibration/protocol_lock_readiness_v1.json` 当前给出 `protocol_lock_allowed=false`、`e02_allowed=false`。阻塞项依次为 pNEUMA 人工第 1 轮、官方 Packet 访问、E17 仿真校准样本和 E01 容差/样本量冻结。任何阻塞项都没有被降级成“已通过”。
