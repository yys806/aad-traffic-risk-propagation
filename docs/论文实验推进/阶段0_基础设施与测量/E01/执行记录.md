# E01 执行记录

## 2026-09-07 预锁验证

预锁包 `code/outputs/formal/calibration/e01_prelock_validation_v2/` 已完成：

- 匀速闭合、静止前车、静止本车、同速行驶的 TTC/DRAC 解析真值一致；
- PET 的非重叠与占用重叠案例通过；
- 碰撞/重叠案例单独识别；
- 物理路径的断开拓扑、有限多车链、右删失和目标车缺失四类判定通过；
- 0.10/0.05/0.02/0.01 s 的解析场景结果已经输出，其中 0.05 与 0.02 s 差为约 0.002987。

该差值尚不能自行判为“足够小”，因为 `delta_eq` 必须由完整 calibration 误差共同冻结。当前 Gate 为 `pending_e15_e17_and_frozen_tolerances`。

协议锁写入器采用 fail-closed 规则：E15-A、E17-A、E01 未通过时拒绝创建 `protocol_lock_v1.1.yaml`；E16-A 若只能得到经审计的有限可观测边界，可用 `pass_limited` 如实锁定，但不能伪造时延、丢包或采用。
