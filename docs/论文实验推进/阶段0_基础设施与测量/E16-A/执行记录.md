# E16-A 执行记录

## 2026-09-07

正式处理器按 `sha256(DeviceID|Trip)` 首字节固定分割 SPMD 行程，只保留 calibration trips 的统计状态，不保存 locked holdout 行程编号或结果。

- calibration：17,481,111 行，7,601 个 device-trip，61,149 个接收状态流；
- locked holdout：17,684,159 行在读取分割键后被直接跳过，`outcome_fields_retained=false`；
- calibration 中大于 1 s 的接收状态断点为 1,254,998，非正时间间隔为 0。

这些数字只描述 `RV_RX` 接收状态连续性。断点不能解释为丢包，行不能解释为消息，也没有控制器采用证据。

USDOT 当前页面仍公开列出 `SPMD.zip`，下载路由为 `/download/deww-aa3r/application%2Fx-zip-compressed`；本机命令行范围请求返回 HTTP 403。因此当前是“官方资产访问待解决”，不是“Packet 字段 Gate 失败”。`Packet` 未取得，`BsmP1` 未下载。

当前 Gate：`pending_official_packet_access`。可观测边界：`receive_state_continuity_only`。
