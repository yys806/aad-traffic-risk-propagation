# 交通风险传播项目交接（2026-07-29）

> 新对话开始时先读本文件，再按这里的“已验证 / 拟采用 / 未完成”边界继续。不要把 pilot 结果写成正式因果结论，也不要把“远端通信”直接写成严格的非局部风险传播或物理超距作用。

## 1. 项目现在到底要研究什么

研究混合交通中，源区域的碰撞风险事件或交通扰动，如何通过车辆响应、交通流或远端信息/控制通道，改变目标区域的碰撞风险。

当前只把碰撞冲突作为风险结果。拥堵、排队和交通波本身不算风险，但可以作为风险的来源或诱因。

现阶段更准确的题目表述是：

> 远端信息介导的跨区域碰撞风险作用与传播机制。

不建议直接使用“超距作用”。交通系统中的远距离影响仍然需要通信、远程感知、导航、中央控制或交通流等机制，不研究无通道、瞬时发生的物理超距作用。

## 2. 当前基本定义

### 2.1 风险事件

一个连续碰撞冲突过程记为

$$
\varepsilon=(i,j,t_s,t_e,r_\varepsilon).
$$

同一车辆对在连续时间内出现的 TTC、THW、制动等指标，是同一风险事件的属性，不能直接重复计为多个传播事件。当前 DRAC 与多指标物理事件统一还没有全部实现。

### 2.2 区域

区域按“拓扑点强制切分 + 拓扑段内固定长度”确定，不能根据风险结果或机制开关临时改变。

第 $m$ 个区域可写为

$$
Z_{b,m}=\{p:b(p)=b,\ a_{b,m}\le s_b(p)<a_{b,m+1}\}.
$$

当前比较过 $L_0=25$、50、75 m。25 m 可作为主分析尺度，50 m 用于敏感性分析；这还不是永久定值。

### 2.3 两类关系

- $E_P$：普通物理传播边，例如跟驰、制动、车辆移动、相邻区域交通波。
- $E_I$：显式信息或控制边，例如跨支路 ETA、远程感知、通信、导航或中央控制信息。

普通局部作用即使经过很多跳、传播得很远，只要存在连续的 $E_P$ 链，仍然属于局部机制的多跳传播。

非局部候选要求源、目标之间超过空间/拓扑邻域，同时存在跳过普通逐级传播的 $E_I$。距离远本身不够。

### 2.4 $D_0$ 与 $H_0$

- $D_0$：路网距离门槛。
- $H_0$：区域拓扑步数门槛。

当前局部直接边的 95% 距离分位约为 27～28 m，因此 $D_0=30$ m 只能作为初始操作值。样本较少，不能定死。距离超过 30 m 也不代表已经是非局部，仍需检查 $H_0$、$E_I$ 和机制干预结果。

## 3. 文献与目前的创新边界

老师发来的文章对应：

- Guo et al. (2026), *Sparse Traffic Accident Risk Forecasting with Spatial-Temporal Knowledge Graphs*, IEEE TKDE。
- DOI：`10.1109/TKDE.2026.3706345`
- Zotero item key：`IFEVUT95`
- 微信介绍：`https://mp.weixin.qq.com/s/tcKoHH9d_MELZiwsR80EkQ`

STKGRisk 使用交通、天气、道路、区域、POI 和事故等宏观数据，通过时空知识图谱、显式/隐式语义图、混合专家和零膨胀混合泊松分布，预测路段与出租分区未来的事故风险。

它与本项目的主要区别：

| 对比 | STKGRisk | 本项目 |
|---|---|---|
| 研究问题 | 未来哪里事故风险高 | 为什么源区域会改变目标区域碰撞风险 |
| 数据 | 历史事故及多源宏观数据 | 闭环仿真车辆轨迹和连续冲突事件 |
| 图的含义 | 实体关系与时空关联 | 分开表示 $E_P$ 和 $E_I$ |
| 证据 | 预测精度 | 指定通道 on/off 配对干预 |

目前可凝练的拟创新：

1. 从区域风险预测转向连续碰撞风险事件的产生和传播机制；
2. 在同一框架内分开表示普通局部物理传播与远端信息/控制作用；
3. 对指定 $A\rightarrow B$ 通道进行同场景、同 seed 的 on/off 实验，估计目标区域风险净变化及效率代价。

这些只是相对现有核心文献的拟创新。是否属于领域首创，下一周还要继续核查风险传播、通信拓扑和交通因果干预文献。

如果只训练一个输入区域历史和图结构、输出未来风险的 GNN，本质上仍然是风险预测，和现有文献差别不大，不能作为本项目核心创新。

## 4. 已完成的实验

本轮为 **Pilot A**，只做了可行性验证。核心输出：

`D:\shen\research-shuqi\code\outputs\nonlocal_pilot_a_20260726`

### 4.1 区域尺度实验

内容：对 p60、p80 的 10 个轨迹文件和 46 条合格风险 episode，分别使用 25、50、75 m 划区。

| $L_0$ | 有车辆区域 | 风险区域 | 成功回连事件 |
|---:|---:|---:|---:|
| 25 m | 39 | 5 | 46 |
| 50 m | 21 | 4 | 46 |
| 75 m | 18 | 4 | 46 |

风险排序的 Spearman 相关：

- 25–50 m：0.796；
- 25–75 m：0.893；
- 50–75 m：0.935。

Spearman 比较的是风险排序是否相似，不是因果关系，也不能直接理解成百分比。

结论：区域算法可运行，主要风险位置对尺度有一定稳定性。25 m 分辨率更高，50/75 m 较粗。该实验不证明风险传播。

### 4.2 ETA 机制开关实验

当前远端机制：合流场景中，一条支路的受控车辆读取另一支路车辆到冲突点的 ETA。双方 ETA 接近时，规则参与速度上限和加速度计算。

实验条件：p60、p80，各运行一次 on 和 shadow-off；配置 seed 为 20260726，实际 SUMO seed 为 13269。

| 渗透率 | on实际应用 | off候选触发 | off实际应用 |
|---:|---:|---:|---:|
| p60 | 651 | 629 | 0 |
| p80 | 749 | 625 | 0 |

解释：

- `would_trigger`：当前车辆状态和 ETA 条件满足，本来应该触发；
- `applied`：该规则真的进入本仿真步的控制计算；
- shadow-off 继续计算和记录候选，但强制 `applied=false`。

“651次应用”的单位是“车辆 × 仿真步”，不是651辆车、651个风险事件或651条独立传播关系。同一辆车连续满足10步会记10次。后续应把连续记录合并为机制作用 episode。

首次机制应用均为 9.6 s，首次轨迹分化均为 9.8 s。机制生效前，on/off 的车辆集合和比较字段完全一致，最大数值差为0。

结论：指定 ETA 通道能够单独开关，轨迹差异发生在机制介入之后。它证明远端信息会影响控制行为，但还不证明风险传播。

### 4.3 初步安全—效率比较

| 指标 | p60 on/off | p80 on/off | 当前解释 |
|---|---|---|---|
| 平均速度 | 13.40 / 14.89 m/s | 13.57 / 15.12 m/s | on约低1.5 m/s |
| 出流 | 1350 / 1440 veh/h | 1350 / 1440 veh/h | on低90 veh/h |
| 碰撞 | 0 / 0 | 0 / 0 | 无法比较 |
| 急刹数 | 1 / 2 | 1 / 2 | on较少 |
| 最小TTC | 1.166 / 0.387 s | 1.419 / 0.350 s | on较高 |
| TTC违规率 | 0.00511 / 0.00272 | 0.00263 / 0.00034 | on反而较高 |

结论：指标方向混合，只能说存在安全—效率权衡。不能声称总体安全收益。

### 4.4 本轮实验总体结论

已经证明：

- 区域划分和风险事件回连可运行；
- ETA 日志字段完整；
- 指定远端通道可以单独 on/off；
- 同 seed 配对在处理前一致；
- 机制介入后轨迹发生变化。

尚未证明：

- 源区域发生了明确风险事件；
- 风险从源区域传播到目标区域；
- 普通 $E_P$ 路径不能解释目标变化；
- 多 seed 下存在稳定因果效应；
- 物理意义上的超距作用。

因此当前准确定位是：

> 非局部信息机制的 pilot 验证，不是严格的非局部风险传播因果验证。

## 5. 下一周建议重点

### 5.1 文献

只针对当前缺口补查，不做泛泛综述：

1. 风险事件和区域风险定义；
2. 局部级联、跨区域风险诱发；
3. 非局部交通流、前视信息和通信拓扑；
4. 网络干扰、边级干预和闭环反事实实验；
5. 是否已有交通安全研究同时完成 $E_P/E_I$ 分层、指定通道干预和源—目标风险效应。

输出一张10～15篇核心文献表，列清“做了什么、能借鉴什么、还缺什么”。

### 5.2 方法框架 V0.1

建议固定为：

1. 风险事件识别；
2. 区域与拓扑构建；
3. 局部物理传播图 $E_P$；
4. 远端信息/控制图 $E_I$；
5. 非局部候选筛选；
6. 指定源—通道—目标的闭环干预；
7. 风险因果效应及安全—效率代价。

当前阶段不需要训练模型。训练风险预测模型只能作为后续候选筛选或预测基线，不能代替干预证据。也不建议重新训练 DRIFT 控制器，以免课题偏成交通流控制优化。

### 5.3 严格实验：2×2设计

同时控制“源区域是否发生扰动”和“远端通道是否打开”：

| 条件 | 源扰动 $S_A$ | 通道 $E_I$ |
|---|---|---|
| 00 | 关 | 关 |
| 01 | 关 | 开 |
| 10 | 开 | 关 |
| 11 | 开 | 开 |

非局部通道的净作用可写为

$$
\Delta_{NL}=(R_B^{11}-R_B^{10})-(R_B^{01}-R_B^{00}).
$$

如果 $\Delta_{NL}>0$ 且稳定，可讨论风险诱发/传播；如果小于0，是风险抑制；接近0则没有证据。

近期可先在现有合流场景中注入可重复的源扰动，例如指定车辆短时减速、到达时间突变或短队列，复用 ETA 通道完成方法验证。

更符合老师问题的正式场景是：

$$
A\text{事故或排队}
\rightarrow\text{导航/云控信息}
\rightarrow\text{改道或集中换道}
\rightarrow B\text{碰撞风险变化}.
$$

该场景需要两个相距较远的瓶颈区域、事件注入、改道/信息模块，工作量明显大于现有合流实验。

### 5.4 正式统计

- p60、p80；
- 2×2四个条件；
- 每个条件至少10个配对seed，共80次；
- 预先固定目标区域主风险指标；
- 报告配对差值、置信区间和效应方向；
- 做错误区域、随机时刻、虚假远端车辆等安慰剂检查；
- 检查机制前一致性、机制后时间顺序以及是否存在及时到达目标的 $E_P$ 路径。

不要在主效应稳定之前铺开 ETA 阈值、通信范围和时延的大参数网格。

## 6. 关键文件

### 研究记录

- 周推进记录：`D:\禹尧珅\人工智能知识库\同济科研\交通风险传播\7.21-7.28推进.md`
- 项目总说明：`D:\shen\research-shuqi\README.md`
- 当前发现记录：`D:\shen\research-shuqi\findings.md`
- 当前进度记录：`D:\shen\research-shuqi\progress.md`

周推进记录已补充老师发的 STKGRisk 文献、三个实验的内容/结果/结论和下一步计划。除非用户明确要求，不要擅自重写前面的定义部分。

### Pilot A 输出

- 汇总：`D:\shen\research-shuqi\code\outputs\nonlocal_pilot_a_20260726\pilot_summary\实验汇总.md`
- 机制汇总：`...\pilot_summary\mechanism_summary.csv`
- 配对一致性：`...\pilot_summary\pair_consistency.csv`
- 指标比较：`...\pilot_summary\metric_comparison.csv`
- 区域实验：`...\region_scale\区域尺度实验.md`
- 原始四组仿真和日志：`...\simulation`

### 代码与环境

- Windows 上游主仓库：`D:\shen\research\code`
- 隔离 worktree：`C:\Users\Lenovo\.config\superpowers\worktrees\code\nonlocal-pilot-a`
- 分支：`experiment/nonlocal-pilot-a`
- WSL 实际运行目录：`/home/shen/shen/mixed_autonomy_lab`
- WSL 预补丁备份：`/home/shen/shen/mixed_autonomy_lab/backups/nonlocal_pilot_a_20260726_prepatch`

截至2026-07-29，隔离分支尚未合并或提交，状态包括：

- 修改：`ours_full_controller.py`
- 修改：`penetration_builders.py`
- 修改：`run_penetration_study.py`
- 新增：`nonlocal_pilot.py`
- 新增：`mixed_autonomy_lab/tests/`

WSL 正式运行副本比 Windows `main` 更旧，本轮只做了备份后的最小补丁移植。禁止用 Windows 文件整包覆盖 WSL 副本。

上次完整验证结果：

- 下游：62 tests passed，compileall通过；
- WSL实际运行代码：12 tests passed，compileall通过；
- 16个汇总文件重复生成后SHA256不变；
- 4个emission、4个机制日志和2个配对检查通过审计。

## 7. 新对话必须保持的表述边界

1. 当前风险结果只看碰撞冲突，不把拥堵直接当风险；
2. 拥堵、事故、排队可以是源区域扰动；
3. 当前 ETA 属于跨支路远端信息交互，不是严格风险传播；
4. `applied` 是“车辆 × 仿真步”的规则应用，不是独立事件；
5. 单 seed pilot 不能写成正式因果结论；
6. 预测精度提升不能替代 on/off 干预；
7. 距离超过 $D_0$ 只是候选条件；
8. 没有及时局部路径、存在明确 $E_I$、干预后目标风险稳定变化，三者同时满足后才能讨论非局部因果作用；
9. 不声称量子或无通道的物理超距作用；
10. 未完成的 DRAC、多指标事件统一、多 seed、安慰剂和新场景不能写成已实现。

## 8. 用户偏好

- 中文；
- 简明、像个人科研记录，不要论文腔和明显AI味；
- 解释公式时要先讲直观含义，再讲符号；
- 明确区分文献已有、我们的操作化定义、代码已实现、实验已验证；
- 修改周推进文档时只改用户指定部分；
- 对不确定内容先核对文件、代码或文献，不猜接口和实验结论。

## 9. 建议的新对话开场

可以直接告诉新对话：

> 请先完整阅读 `D:\shen\research-shuqi\下一周对话交接_2026-07-29.md`，再读取其中列出的周推进记录和 Pilot A 汇总。接下来进入下一周，先做针对性文献调研和方法框架 V0.1，重点解决“怎样从远端 ETA 信息交互升级为严格的源区域—通道—目标区域风险传播验证”。不要直接修改代码，先把研究问题、2×2实验定义和主要风险指标定清楚。
