# 毕业设计与论文课题固化

本文档用于固定本项目的最终目标：本课题既作为毕业设计主线，也作为后续论文发表方向。后续实验、代码开发、文献综述和论文写作均围绕该主题展开，避免继续在无关方向上发散。

## 课题题目

混合自动驾驶碰撞风险的局部多跳与非局部作用辨识及反事实验证

可作为论文题目的英文表述：

Identifying Local Multi-Hop and Nonlocal Effects of Collision Risk in Mixed-Autonomy Traffic via Counterfactual Simulation

## 背景

在混合自动驾驶交通中，人类驾驶车辆（HV）和自动驾驶车辆（AV）会长期共存。现有研究通常关注单车风险、局部冲突、轨迹预测误差、通行效率或整体安全指标，例如 TTC、THW、急刹次数、平均速度和碰撞率等。

但在真实交通演化中，一个局部风险事件往往不是孤立发生的。一次急刹、危险合流或低 TTC 可能通过跟驰、换道、队列传播和 AV 策略响应，继续影响后续车辆、相邻车道或更远位置。拥堵和激波是传播背景或中介机制，不作为本文主要风险类型。

因此，本课题不再把重点放在继续增强 DRIFT 生成模型本身，而是把 DRIFT/Flow 作为闭环交通演化与反事实验证平台，在已有 rollout、candidate execution、risk metrics 和 AV penetration 对比基础上，进一步研究风险事件如何在混合自动驾驶交通系统中传播、放大、衰减和被干预。

## 解决的问题

本课题拟解决以下问题：

1. 如何从 DRIFT/Flow 的闭环 rollout 数据中抽取碰撞/冲突风险事件，例如 hard braking、low TTC、low THW、near-miss、unsafe merge 和 cut-in。

2. 如何判断这些风险事件之间是否存在传播关系，而不是只把它们看成彼此独立的局部现象。

3. 如何区分相邻车辆逐级传递的局部多跳传播、由前视感知或通信控制支持的非局部候选作用，以及共同交通状态造成的伪相关。

4. 如何识别风险源、风险放大器和风险吸收器，即哪些车辆、事件或区域更容易触发后续风险，哪些会放大风险，哪些会让风险链衰减或终止。

5. 如何通过反事实重仿真验证关键风险链是否可以被削弱或切断，例如平滑急刹、调整合流时机、改变 AV 让行策略或改变 AV penetration。

## 提出的方法

本课题拟采用“事件抽取 - 风险传播图建模 - 传播指标计算 - 反事实干预验证”的整体方法。

首先，基于 DRIFT/Flow 的 closed-loop rollout 输出，读取车辆在连续时间中的位置、速度、加速度、headway、leader、lane、TTC 和 THW 等信息，抽取风险事件，并形成 `risk_events.csv`。

其次，构建 vehicle-event-region 风险传播图。图中的节点包括车辆节点、风险事件节点和道路区域节点；边先作为观察性候选关系，依据时间先后、空间距离、跟驰关系、换道/合流关系、同一区域、前视感知范围和通信/控制范围生成。局部相邻图作为多跳基线，远距离边必须控制中间车辆历史、道路状态和共同控制信号。

然后，计算 propagation depth、propagation distance、propagation delay、amplification ratio、decay rate、source score、amplifier score 和 absorber score。远距离关系还要经过条件独立性检验、时间与车辆编号打乱等负对照，区分多跳路径和非局部候选作用。这些指标和检验用于筛选干预对象，不直接作为因果结论。

最后，对高 source/amplifier 分数的车辆、事件或道路区域进行反事实干预，并重新 rollout。干预方式包括平滑急刹、改变 unsafe merge 时机、调整 AV yielding policy 或屏蔽特定远端信息。通过固定场景和随机种子，分别干预源节点与中介节点，比较后续风险和效率副作用，判断远距离关系属于多跳传递还是具有独立机制的非局部作用。

## 核心假设及边界

本课题把“超距作用”作为待验证假设，不作为预设结论。这里的超距指：两个车辆不满足直接跟驰或局部接触关系，但源车辆的历史状态在控制中间车辆和共同交通条件后，仍能稳定解释目标车辆的碰撞风险；同时必须找到前视感知、V2V/V2I、路侧信息或集中控制等交通机制，并通过闭环干预复核。

量子决策与量子式概率文献只用于补充驾驶人的情境依赖、非理性和交互不确定性建模。除非存在可测变量、作用通道和可证伪预测，论文不把量子理论写成交通系统的物理传播机制。

## 得到的结果

正式论文实验结果待补充。

目前已有的初步可行性结果如下，仅用于说明方向可以落地，不作为最终结论：

1. 已在服务器上跑通真实 Flow merge smoke，并接入本地 `riskprop` 分析流程。

2. 当前最小链路已经实现：真实 Flow rollout emission -> 风险事件抽取 -> 风险传播边构建 -> 风险链与 summary 输出。

3. 在一次真实 Flow merge smoke 中，检测到 309 个逐帧风险状态；将连续同车、同类型检测合并后得到 16 个风险事件片段，并从 37 条候选关系中按规则保留 18 条边。筛选边的平均延迟为 1.467 s，平均距离为 13.679 m；这些边只表示观察性候选关系。

4. 当前真实 emission 缺少有效 lane 字段，因此未验证的长距离同车道边已被剔除，现阶段不能对非局部超距传播作出结论。后续仍需完成多场景、多 AV penetration、多随机种子和反事实干预实验。

## 结论

正式结论待完成系统实验后补充。

后续结论应重点回答：

1. 混合自动驾驶交通中风险事件是否会形成稳定、可解释的传播链。

2. AV penetration、AV 策略和关键 HV 行为是否会显著改变风险传播模式。

3. 所提出的风险传播图和反事实干预方法，是否能够比传统全局安全指标更清楚地解释风险来源、传播路径和可干预位置。

4. 该方法是否具备作为毕业设计完整系统和论文投稿工作的实验支撑。
